const contactInitialState = {
    contacts: [],
};
const contacts = (state = contactInitialState, action) => {
    switch (action) {
        default:
            return state;
    }
};

export default contacts;
